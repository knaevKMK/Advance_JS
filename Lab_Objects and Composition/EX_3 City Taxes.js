function solve(name, population,treasury) {
    const towns={
        name: name,
        population: population,
        treasury: treasury,
        taxRate: 10,
        collectTax: [Function: collectTax],
    applyGrowth: [Function: applyGrowth],
    applyRecession: [Function: applyRecession]


};
}